# 아임웹 / 식스샵 코드위젯 제작 규칙

이 레포에서 "코드위젯"을 만들어달라는 요청은 모두 아임웹(또는 식스샵) 웹빌더의
코드위젯에 그대로 붙여넣을 수 있는 **body-only HTML 스니펫**을 의미한다.
아래 규칙을 항상 지킨다.

## 필수 규칙

1. `<!doctype>`, `<html>`, `<head>`, `<body>`, `<meta>`, `<title>` 태그를 절대 포함하지 않는다.
   코드위젯은 플랫폼이 만든 페이지의 `<body>` 중간에 그대로 삽입되는 조각이다.
2. CSS는 `<style>`, JS는 `<script>` 태그 안에만 작성한다. 외부 CSS/JS 파일을 따로
   만들지 않는다(폰트/아이콘 CDN `<link>` 태그는 예외로 허용).
3. 모든 CSS 셀렉터는 **ID 우선** + 브랜드/섹션 이름을 접두사로 붙인다.
   예: `#brand-cards-root .brand-cards-card`. 플랫폼 기본 스타일이나 같은 페이지의
   다른 위젯과 클래스명이 충돌하지 않도록 하는 것이 목적이다.
4. 아이콘은 `lucide-react`나 인라인 SVG 대신 **Font Awesome**(CDN)을 사용한다.
   ```
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
   ```
5. `h1`~`h6` 태그를 사용할 경우, 아임웹 기본 테마가 헤딩에 자체 스타일을 입히므로
   리스타일링 전에 반드시 `all: unset !important`로 초기화한다.
6. 이미지가 필요한 경우 깨진 링크/플레이스홀더가 아닌 실제 로드되는 이미지
   (Unsplash 등)를 사용한다.
7. 사용자가 참고 이미지/디자인을 준 경우, 그 디자인의 레이아웃·색감·구성을
   최대한 그대로 재현한다.
8. 브랜드명이 주어지지 않았다면 명확히 placeholder임을 표시하고 (`brand-` 같은
   접두사) 채팅 답변에서 실제 브랜드명으로 교체가 필요하다고 안내한다.
9. 작업자/제작 스튜디오 명의나 워터마크(예: 딧불, ditbool, 브릿지그램 등)는
   코드 어디에도 포함하지 않는다.

## 작업 흐름

1. `*-widget-body.html` 형식의 새 파일로 작성한다 (예: `nexova-404-widget-body.html`).
2. 작업 후 현재 브랜치에 커밋·푸시한다.
3. 채팅 답변에는 전체 코드를 ` ```html ` 코드블록으로 붙여넣어, 사용자가 그대로
   복사해서 아임웹/식스샵 코드위젯에 붙여넣을 수 있게 한다.
4. placeholder(브랜드명, 이미지, 링크 등)가 있다면 답변에서 명확히 짚어준다.

## 참고: 프레임워크 변환

React/Tailwind/lucide-react 등으로 작성된 컴포넌트 요청이 들어오면, 위 규칙에
맞춰 **순수 HTML/CSS/바닐라 JS**로 다시 작성한다. Tailwind 유틸리티 클래스는
동일한 CSS 속성으로, `lucide-react` 아이콘은 대응되는 Font Awesome 아이콘으로
치환한다.
